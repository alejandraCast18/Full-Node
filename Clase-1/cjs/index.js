/* console.log(globalThis)
es una variable global de toda la aplicacion, ademas
console.log es una propiedad de la variable globalThis*/

//common js require module//

const {sum} = require("./sum")
console.log(sum(1,2))
